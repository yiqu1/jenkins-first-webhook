pipeline {
    agent any
    stages {
        stage("build"){
	    steps {
		sh "echo Integratng Jenkins pipeline with github webjook using jenkinsfile"
	   	sh "python --version"
	   	sh "phthon pipeline.py"
	    }
	}   
    }
}
